package entities;

public class BuscaBinaria<T extends Comparable<T>> extends BuscaAbstract {
	public int buscar(T valor) {
		int n = getInfo().length - 1;
		int inicio = 0;
		int fim = n;
		while(inicio <= fim) {
			int meio = (inicio + fim) / 2;
			if(valor.compareTo(getInfo()[meio]) < 0) {
				
			}
		}
		return -1;
	}
}
