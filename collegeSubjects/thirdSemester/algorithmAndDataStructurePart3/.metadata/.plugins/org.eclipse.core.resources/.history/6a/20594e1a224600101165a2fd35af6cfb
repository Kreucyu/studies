
package application;

import entities.OrdenacaoAbstract;
import entities.OrdenacaoBolha;
import entities.OrdenacaoBolhaOtimizada;

public class App {
	public static <T> void main(String[] args) {
		Integer info[] = {70, 2, 88, 15, 90, 30};
		
		//CASO 1:
		OrdenacaoAbstract<Integer> bubble = new OrdenacaoBolha<>();
		bubble.setInfo(info);
		bubble.ordenar();
		System.out.print("Lista bubbleSort: { ");
		int i = 0;
		int n = bubble.getInfo().length - 1;
		while(i <= n) {
			System.out.print(bubble.getInfo()[i]);
			if(i != n) {
				System.out.print(", ");
			}
			i++;
		}
		System.out.print(" }");
		
		//CASO 2:
		OrdenacaoAbstract<Integer> bubbleOtimizado = new OrdenacaoBolhaOtimizada<>();
		bubbleOtimizado.setInfo(info);
		bubbleOtimizado.ordenar();
		System.out.print("Lista bubbleSort: { ");
		int i = 0;
		int n = bubbleOtimizado.getInfo().length - 1;
		while(i <= n) {
			System.out.print(bubbleOtimizado.getInfo()[i]);
			if(i != n) {
				System.out.print(", ");
			}
			i++;
		}
		System.out.print(" }");
	}
	
	private void teste(Integer vetor[], OrdenacaoAbstract<Integer> ordenador)
}
